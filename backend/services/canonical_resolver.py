from services.canonical_track_resolver import (
    group_duplicate_tracks as group_duplicate_songs,
    is_compilation_album,
    normalize_song_identity as normalize_song_key,
    resolve_canonical_tracks as resolve_canonical_versions,
    select_canonical_track as select_canonical_song,
)
